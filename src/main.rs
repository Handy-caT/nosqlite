#![feature(core_intrinsics)]

mod core;
mod data;
mod page;
mod schema;

fn main() {
    // let bytes: [u8; 8] = [0, 0, 0, 0, 0, 0, 1, 125];
    // let num = u64::from_be_bytes(bytes);
    // println!("{}", num);
}
